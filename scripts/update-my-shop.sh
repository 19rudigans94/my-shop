#!/bin/bash

# Скрипт для обновления проекта на сервере
# Использование: ./update-my-shop.sh

set -e  # Прерывать выполнение при ошибке

# Определяем путь к проекту
# Если скрипт запущен из папки scripts, переходим на уровень выше
# Иначе используем домашнюю директорию
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

# Проверяем, что мы в правильной директории (должен быть package.json)
if [ ! -f "$PROJECT_DIR/package.json" ]; then
  # Если не нашли, пробуем домашнюю директорию
  PROJECT_DIR="$HOME/my-shop"
  if [ ! -f "$PROJECT_DIR/package.json" ]; then
    echo '❌ Папка проекта my-shop не найдена'
    echo "Искали в: $PROJECT_DIR"
    exit 1
  fi
fi

echo '📁 Переход в папку проекта...'
echo "Путь: $PROJECT_DIR"
cd "$PROJECT_DIR" || { echo '❌ Не удалось перейти в папку проекта'; exit 1; }

echo '🔄 Обновление из Git...'
git fetch origin main

echo '🌿 Переключение на ветку main...'
git checkout main || git checkout -b main origin/main

echo '🗑 Очистка локальных изменений...'
git reset --hard origin/main
git clean -fd

echo '✅ Репозиторий синхронизирован с origin/main'

echo '📦 Установка зависимостей...'
npm ci --prefer-offline --no-audit || npm install

echo '🛠 Сборка проекта...'
npm run build

echo '🚀 Перезапуск сервиса через PM2...'
pm2 restart my-shop || pm2 start npm --name my-shop -- start

echo '✅ Деплой успешно завершён.'
